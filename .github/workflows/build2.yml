name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY2 }}

      - name: Deploy to VPS
        run: |
          ssh -o StrictHostKeyChecking=no root@45.87.43.110 << 'EOF'
            cd /var/www/roxxiestreams
            git pull origin main
            # Optionally restart services or perform additional tasks
            sudo systemctl restart nginx
          EOF
